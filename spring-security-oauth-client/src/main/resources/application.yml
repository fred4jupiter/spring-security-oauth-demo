spring:
  application:
    name: "spring-security-oauth-client"

  security:
    oauth2:
      client:
        registration:
          demo-client:
            client-id: "demo-client"
            client-secret: "4jyvCXMBIgrSaKthGDEidMpQFniVBDPi"
#            scope: profile
            authorization-grant-type: "client_credentials"
            provider: "keycloak"
        provider:
          keycloak:
            issuer-uri: "http://localhost:8380/realms/summer"
            jwk-set-uri: "${spring.security.oauth2.client.provider.keycloak.issuer-uri}/protocol/openid-connect/certs"
            token-uri: "http://localhost:8380/realms/summer/protocol/openid-connect/token"
            authorization-uri: "http://localhost:8380/realms/summer/protocol/openid-connect/auth"

server:
  port: 8180